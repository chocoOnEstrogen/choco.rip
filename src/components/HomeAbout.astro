---
import { getCollection } from "astro:content";

const about = (await getCollection("misc")).find(
    (post: any) => post.slug === "short_about"
);

if (!about) {
    throw new Error("Could not find short_about content");
}

const { Content } = await about.render();
---

<p>
    <Content />
</p>