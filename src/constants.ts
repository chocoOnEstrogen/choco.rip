
import config from '@/cfg'

export const APP_NAME = config.app.name

export const DEFAULT_SEO = {
	description: config.seo.description,
	twitterHandle: config.seo.twitter_handle,
	keywords: config.seo.keywords,
	author: config.seo.author,
	type: config.seo.type,
}

export const NAVBAR_BRAND = config.app.navbar_brand
export const USERNAME = config.app.username
export const USER_DESCRIPTION = config.app.user_description
